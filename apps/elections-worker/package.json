{
  "name": "@elections-tracker/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file-if-exists=.env dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "ingest": "tsx --env-file-if-exists=.env src/index.ts",
    "assess:backfill": "tsx --env-file-if-exists=.env src/backfill-assessments.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@elections-tracker/shared": "workspace:*",
    "fflate": "^0.8.3",
    "nodemailer": "^9.0.5",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/nodemailer": "^8.0.1",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.11"
  }
}
